@model IEnumerable<Football.App.ViewModels.MatchViewModel>

@if (Model != null && Model.Any())
{
    @foreach(var match in Model)
    {
        <div class="card m-3">
            <div class="d-flex flex-row align-items-center justify-content-center text-center">
                <h4 class="col-md-5 p-2 mx-2">@match.Team1Player1Name + @match.Team1Player2Name</h4>
                <div class="col-md-2 d-flex flex-column align-items-center justify-content-center" style="height: max-content">
                    <h5 class="m-0">@match.Score1 : @match.Score2</h5>
                    <p class="m-0">@match.Date.ToShortDateString()</p>
                </div>
                <h4 class="col-md-5 p-2 mx-2">@match.Team2Player1Name + @match.Team2Player2Name</h4>
            </div>
        </div>
    }
}
else
{
    <p>No matches available.</p>
}